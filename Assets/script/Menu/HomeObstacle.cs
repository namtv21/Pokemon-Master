using UnityEngine;

/// <summary>
/// Marker gắn lên đồ nội thất trong Nhà Pokemon (giường, tủ...).
/// Vùng cản = chính RectTransform của ảnh nội thất — Pokemon sẽ né khi đi lại.
/// </summary>
[RequireComponent(typeof(RectTransform))]
public class HomeObstacle : MonoBehaviour
{
}
